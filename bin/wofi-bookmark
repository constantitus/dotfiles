#!/bin/bash
if [ $? -eq 0 ]
then
    sed '1,/^### DATA ###$/d' $0 | wofi --show dmenu -i | tr -d '\n' | wtype -
else
    sed '1,/^### DATA ###$/d' $0 | wofi --show dmenu -i | tr -d '\n' | wl-copy
fi
exit
### DATA ###
https://github.com/
emerge -uvDNaq --with-bdeps=y --keep-going=y @world

